export class Student {
   constructor(public id: string, public fullName: string, public email: string, 
    public university: string, public age: number, public gender: string, 
    public imgUrl: string,  public country: string, public isSelected: boolean) {
    }
}
